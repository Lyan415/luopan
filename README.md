# 🧭 後天八卦二十四山風水羅盤

![version](https://img.shields.io/badge/version-v1.8.0-d8a94f)
![platform](https://img.shields.io/badge/platform-Web%20%7C%20iOS%20%7C%20Android-8c1d18)
![dependencies](https://img.shields.io/badge/dependencies-none-4a7c59)

一個單一 HTML 檔案的手機版電子羅盤工具。可將傳統二十四山羅盤疊加在自行上傳的平面圖或現場照片上，用於格局定向、方位標註與截圖記錄。

無需安裝、無需後端、無外部相依套件，開啟網頁即可使用。

**線上使用**：https://Lyan415.github.io/luopan/

---

## 功能

| 功能 | 說明 |
|---|---|
| ⚫ 中心定位 | 羅盤中心黑點為定位基準，磁針紅色軸心恆與其重合 |
| 🧲 模擬磁針 | 天池內的磁針有三種模式：**A 刻度**（牛角指向輸入的羅盤刻度）、**B 底圖**（牛角指向底圖真北＋磁偏角，隨底圖旋轉）、**C 指南針**（依手機指南針即時指向磁北） |
| 🔍 自由縮放 | 雙指縮放，或使用 `＋` / `－` 按鈕 |
| 🔄 自由旋轉 | 雙指扭轉，或使用 `↺15` / `↺1` / `1↻` / `15↻` 按鈕微調 |
| ✋ 位置調整 | 直接拖曳移動，或用方向鍵微調；`◎` 鍵可只重設位置 |
| 🎯 度數定向 | 輸入 0–359.9 任一度數，該度數立即轉至畫面正上方 |
| 🧭 手機指南針 | 顯示手機頂端目前朝向的度數；可一鍵將該方位轉至正上方 |
| 📡 自動跟隨 | 開啟後羅盤隨手機轉動即時對正實際方位，如同實體羅盤 |
| 🎚️ 底色濃度 | 0–100% 無段調整羅盤扇區底色的透明度，**刻度、文字與線條永遠維持不透明**，方便透視底圖 |
| ✒️ 文字描邊 | 所有文字皆有自動配色描邊，底色調透明後在深色或淺色底圖上都清晰可辨 |
| 🖼️ 底圖編輯 | 上傳圖片作為底圖，可拖曳、縮放、旋轉；畫面所見範圍即為裁切結果 |
| 📐 底圖方位 | 上傳底圖時設定「圖上方的真方位角」（預設 0°）與磁偏角（預設偏西 5°），附磁偏角查詢連結 |
| ✌️ 手勢鎖定 | 左上角切換雙指手勢為「縮＋轉／僅縮放／僅旋轉」，全部圖層共用 |
| 🔁 更換底圖 | 隨時重新上傳替換，羅盤設定不受影響 |
| ✛ 紅色十字線 | 每次新增一組細十字線（上限 12 組）：主軸實線、垂直軸虛線，恆保持 90°；紅點沿天池邊緣標示主軸所向 |
| 🏔️ 二十四山坐向 | 依紅點所指的羅盤刻度自動換算坐向，例：紅點在 0° → 坐午向子 |
| 🔗 群組變換 | 「全部」頁籤可將底圖＋羅盤＋十字線視為同一整體，一起位移／旋轉／縮放，各層相對關係完全不變 |
| 💾 存成圖片 | 輸出 PNG。輸出圖**不含任何功能鍵**，但會保留羅盤角度、手機方位與各紅線角度 |
| 👁️ 淨空模式 | 一鍵隱藏所有操作介面，全螢幕檢視 |
| 🌓 背景深淺 | 深色／淺色底切換 |

---

## 使用方式

### 在手機上開啟

直接以手機瀏覽器開啟網址即可。建議加入主畫面，使用起來與原生 App 相同：

- **iPhone（Safari）**：分享鍵 → 加入主畫面
- **Android（Chrome）**：右上角選單 → 加到主畫面

### 基本操作流程

1. 切到 **🖼️ 底圖** 頁籤 → 上傳平面圖或現場照片 → 輸入圖上方的真方位角與磁偏角 → 拖曳／縮放／旋轉調整到定位
2. 切到 **☯ 羅盤** 頁籤 → 調整羅盤大小與位置，使中心黑點對準量測基準點；拉動「底色濃度」滑桿讓底圖透出來
3. 定向，擇一方式：
   - 直接輸入度數，按「轉到正上方」
   - 切到 **🧭 磁針** 頁籤 → 啟用手機指南針 → 按「將手機目前方位轉到正上方」，或開啟「羅盤自動跟隨」
   - 在 B 模式下，旋轉羅盤直到磁針牛角對齊海底線兩紅點，即為實際座向
4. 需要標示座向時，切到 **✛ 十字線** 頁籤新增十字線，拖曳畫面即可整組旋轉
5. 按 **💾 存圖** 輸出結果

---

## 度數對應

羅盤圖面採傳統 **南上北下** 配置，子山 0° 位於圖面下方，度數在圖面上以逆時針方向遞增（午 180° 在上、卯 90° 在左、酉 270° 在右）。

「轉到正上方」的換算即依此圖面配置計算，輸入的度數會精準對應到圖上實際印製的刻度。

---

## 方位感測說明

「方位」功能依賴裝置的磁力感測器，有以下限制：

- 必須在 **HTTPS** 環境下開啟（GitHub Pages 已符合；以 `file://` 本機開啟則無法使用）
- iOS 會跳出「動作與方向」權限請求，需允許
- 部分 App 內嵌瀏覽器（如社群軟體內建瀏覽器）取不到感測資料，請改用 Safari 或 Chrome 開啟
- 磁力感測易受金屬結構、電子設備、磁鐵干擾，讀數僅供參考；精密定向請以實體羅盤或專業儀器為準

若裝置不支援，其餘所有功能（含手動輸入度數定向）仍可正常使用。

---

## 存圖行為

輸出時會另以離屏畫布重繪一次，因此**操作介面不會出現在圖片中**，僅保留左上角的角度資訊。

依執行環境不同，存檔方式為：

- **iOS**：跳出分享選單 → 選「儲存影像」存入相簿
- **Android**：直接下載至相簿／下載資料夾
- 若瀏覽器不支援上述方式，可改用手機截圖功能保存（「淨空模式」可先隱藏介面）

---

## 部署

本專案為單一 HTML 檔，部署到 GitHub Pages 的步驟：

1. 建立一個新的 repository（Public）
2. 上傳 `index.html` 至根目錄
3. Settings → Pages → Source 選擇 `Deploy from a branch`，Branch 選 `main` / `(root)`，Save
4. 約一分鐘後即可透過 `https://<帳號>.github.io/<repo 名稱>/` 存取

亦可直接下載 `index.html` 以任何瀏覽器開啟離線使用（方位感測功能除外）。

---

## 技術說明

- 純前端單一檔案，無框架、無 CDN、無外部相依
- 羅盤圖形為 SVG，以 Base64 內嵌於 HTML 中，離線亦可正常顯示
- 羅盤拆為「底色層」與「線條文字層」兩張 SVG 疊繪，透明度僅作用於底色層，因此調整濃度時文字與刻度仍完全清晰
- 文字描邊由羅盤 SVG 內建（白／黑雙層描邊），底色調透明時在任何底圖上都能辨識
- 磁針圖以 10 倍解析度內嵌，放大時仍保持銳利
- 繪圖使用 HTML5 Canvas，底圖與羅盤為獨立圖層，各自保有位置／縮放／旋轉狀態
- 手勢以 Pointer Events 實作，支援單指拖曳與雙指縮放旋轉
- 重繪以 `requestAnimationFrame` 節流，確保大張照片下仍維持流暢
- 方位讀取優先採用絕對方位事件（`deviceorientationabsolute` / `webkitCompassHeading`）

**上傳的底圖僅在瀏覽器本機處理，不會上傳至任何伺服器。**

---

## 版本紀錄

版號同時顯示於 APP 右上角讀數面板（點擊可查看更新內容）、瀏覽器分頁標題，以及輸出圖片的右下角。

### v1.8.0 — 2026-09-21
- 十字線改細；主軸為實線、垂直軸為虛線（線寬不受群組縮放影響）
- 方位紅點改為沿天池邊緣繞行
- 新增二十四山坐向顯示（讀數面板、十字線頁、輸出圖），刻度依羅盤印製度數計算

### v1.7.0 — 2026-09-21
- 更換羅盤圖（新版含天池、海底線與內建文字描邊），移除舊版自加的中心紅點
- 新增模擬磁針：A 刻度／B 底圖／C 指南針三種模式；有底圖時預設 B，可手動切換
- 上傳底圖時設定圖上方真方位角與磁偏角，附 magnetic-declination.com 查詢連結；可於底圖頁重新編輯
- 新增雙指手勢鎖定（縮＋轉／僅縮放／僅旋轉），全部圖層共用
- 修正：群組旋轉後，羅盤與底圖的方向鍵及單指拖曳改依手機畫面方向移動

### v1.6.0 — 2026-09-18
- 新增「🔗 全部」群組模式：底圖、羅盤、十字線可綁在一起同步位移、旋轉、縮放
- 群組旋轉後，讀數面板與輸出圖片顯示的角度皆自動換算為畫面實際角度
- 「輸入度數轉到正上方」與十字線拖曳定向皆已補償群組旋轉量

### v1.5.0 — 2026-09-18
- 紅色參考線改為垂直十字線，兩軸隨旋轉恆保持 90°
- 每組十字線僅保留一個紅點，標示主軸方位角

### v1.4.0 — 2026-09-18
- 全部 100 個文字元素加上自動配色描邊（深色字配白邊、淺色字配深邊）
- 卦符描邊寬度特別收細，避免三畫糊成一團

### v1.3.0 — 2026-09-18
- 新增羅盤底色濃度調整（0–100% 無段），文字與刻度維持不透明
- 羅盤圖形拆分為底色層與線條文字層兩張 SVG 疊繪

### v1.2.0 — 2026-09-18
- 可視區改為全螢幕，控制介面改為底部半透明浮層，按鈕縮小
- 新增淨空模式（一鍵隱藏介面）與方位自動跟隨
- 輸出圖片改以離屏畫布重繪，不含功能鍵、保留角度值
- 修正 6 項問題：雙指放開一指時畫面暴衝、Android 方位讀數被相對值覆蓋、輸出圖與畫面範圍不符、底圖／紅線未就緒時按鍵無回饋、大圖拖曳卡頓

### v1.1.0 — 2026-09-18
- 新增羅盤位置方向鍵微調

### v1.0.0 — 2026-09-18
- 初版：中心紅點、自由縮放旋轉、度數定向、方位感測、底圖編輯與更換、紅色參考線、存成圖片

---

## 授權

（請自行填入，例如 MIT License）
